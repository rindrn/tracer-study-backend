$kind: http-request
name: GET /alumni/me
description: |-
  Guard: alumni
  Controller: App\Http\Controllers\Api\Alumni\SelfServiceController@me
url: "{{baseUrl}}/alumni/me"
method: GET
headers:
  Accept: application/json
  Authorization: Bearer {{alumniToken}}
scripts:
  - type: afterResponse
    code: |-
      pm.test('tidak error server', () => pm.expect(pm.response.code).to.be.below(500));
      pm.test('respons JSON', () => pm.response.to.be.json);
    language: text/javascript
order: 10000
