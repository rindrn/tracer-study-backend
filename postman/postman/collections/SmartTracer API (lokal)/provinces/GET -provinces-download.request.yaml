$kind: http-request
name: GET /provinces/download
description: |-
  Guard: public
  Controller: App\Http\Controllers\Api\Transactional\RegionController@downloadProvinces
url: "{{baseUrl}}/provinces/download"
method: GET
headers:
  Accept: application/json
scripts:
  - type: afterResponse
    code: |-
      pm.test('tidak error server', () => pm.expect(pm.response.code).to.be.below(500));
      pm.test('respons JSON', () => pm.response.to.be.json);
    language: text/javascript
order: 5000
