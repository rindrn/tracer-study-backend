$kind: http-request
name: POST /provinces
description: |-
  Guard: staff
  Peran yang diizinkan: head_tracer
  Controller: App\Http\Controllers\Api\Transactional\RegionController@storeProvince
  Sumber aturan: validate() di controller

  Field body:
  - code (wajib): required, string, max:10, unique:oltp.provinces,code
  - name (wajib): required, string, max:100
url: "{{baseUrl}}/provinces"
method: POST
headers:
  Accept: application/json
  Content-Type: application/json
  Authorization: Bearer {{token}}
body:
  type: json
  content: |-
    {
      "code": "KODE01",
      "name": "Contoh"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test('tidak error server', () => pm.expect(pm.response.code).to.be.below(500));
      pm.test('respons JSON', () => pm.response.to.be.json);
    language: text/javascript
order: 2000
